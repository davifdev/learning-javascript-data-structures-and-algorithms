export class Node {
  constructor(key) {
    this.key = key; // valor do nó
    this.left = null; // referência ao nó que é o filho á esquerda
    this.right = null; // referência ao nó que é o filho á direita
  }
}
